<!-- custom_edittext_error_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- White background (visible when there's an error) -->
    <item android:id="@+id/errorBackground">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Set the white background color here -->
            <stroke
                android:width="2dp"
                android:color="@android:color/holo_red_light" />
            <corners android:radius="8dp" /> <!-- Set the border radius here -->
        </shape>
    </item>
</layer-list>
